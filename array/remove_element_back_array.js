let testArray = [1, 2, 3, 4, 5];

// remove integer 5 from the back of testArray using .pop
let newArray = testArray.pop(5);

// print testArray which doesn't contain integer 5
console.log(testArray);
