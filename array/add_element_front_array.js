let testArray = [1, 2, 3, 4, 5];

// add integer 0 in the front of testArray using .unshift
let newArray = testArray.unshift(0);

// print testArray which contain the new integer 0
console.log(testArray);
