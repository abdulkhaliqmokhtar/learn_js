// create function that always divide the inputNumber by 5, and return the answer
function divideByFive(inputNumber) {
  return inputNumber / 5;
}

// call the function with input value of 100 and store the calculation result in a answer variable
let answer = divideByFive(100);

// print value stored in answer variable
console.log(answer);
